# experiment 分支 Web 实验与 Game 开发基线

本文件是 `experiment` 分支的实施与验收基线。它把需求文档中的研究目标
落实为可运行、可调参、可复现实验，并提供面向玩家的独立三代 Game 模式。
生态淘汰子实验覆盖可耗竭浮游、饥饿与真实灭绝结局；Game 模式在相同模拟
内核上实现调参、演算、结算和成功继承，但不暴露完整调试参数。

## 1. 范围与红线

- 运行入口是根目录 Vite 应用，渲染使用 Three.js，动态刚体使用
  `@dimforge/rapier3d-compat@0.19.3`。
- 不引入 Godot、Electron、Tauri 或任何桌面套壳。
- 实验调试模式与 Game 模式相互隔离。前者没有关卡、轮次或代际继承；后者
  使用专属玩家 UI 和三关状态机。
- 默认三群数量为小/中/大 `400/200/80`。
- 主水族馆默认缸体为 `6.0 × 3.6 × 2.4`；障碍和生态子实验继续使用
  各自独立预设。
- 实验调试层分为主水族馆、地图刚体和生态淘汰三个项目上下文。子实验不得
  反向修改主水族馆的默认生物规则。营养级联及其 holding/判据已删除。
- 提供 `200/80/40` 的性能预设；手工改数量进入 Custom，不偷偷
  覆盖用户值。
- `REQUIREMENTS-experiment.md`、`赛道信息.md`、`docs/`、`test/`、`godot/`
  继续由 `.gitignore` 排除；本地 `地图演示图.png` 只作布局参考。
- 所有实验参数必须进入统一配置和面板注册表。行为代码不得藏有只能改源码
  才能调节的实验常量。Game 玩家只操作三个经过翻译的参数；完整注册表继续
  供实验调试与参数导出使用。

## 2. 系统边界

每个固定步只建立一次全局空间哈希。鱼以联合数组存储，并以 `schoolId`
区分群体；同群 Reynolds 力、跨群斥力、捕食关系与威胁均由同一轮邻居遍历
累加。静态障碍通过预计算距离场查询，Rapier 仅处理缸壁、静态碰撞体和少量
动态刚体，鱼本身不注册刚体。

```text
seeded RNG → 联合鱼群 → 全局空间哈希 → steering / 捕食关系
                    ↘ 距离场 → 静态避障
                    ↘ Rapier → 动态物体查询与偏心冲量
                    ↘ EcologyLedger → 浮游、能量、死亡与胜者
```

配置分为 `live`、`reset`、`rebuildField`、`rebuildScene` 四种生效方式。
JSON 导入先完整校验，再一次性替换运行配置；模拟和面板不能处于不同版本。

## 3. 尺度、动态角色与捕食

同群感知半径从目标邻居数反推：

```text
rawRadius = cbrt(3 × min(targetNeighbors, count - 1) × tankVolume
                 / (4π × count))
visualLength = 0.046 × size
neighborRadius = max(rawRadius, minNeighborRadiusFactor × visualLength)
```

凝聚、对齐、分离、威胁探测半径分别是邻域半径的
`1 / 0.533 / 0.222 / 0.511` 倍。跨群斥力半径为
`0.15 × max(sizeA, sizeB)`。空间哈希 cell 使用所有查询半径中的最大值，
统一查询 `3×3×3` 邻格。

鱼群没有固定的 predator/prey 类型。任何 `size` 修改都会在下一固定步
重新计算完整关系矩阵。

主水族馆默认采用带上界的体型窗口：

```text
k ≤ actorSize / targetSize ≤ KMax       → pursuit
1/KMax ≤ actorSize / targetSize ≤ 1/k   → evade
1/k < actorSize / targetSize < k        → peer
其他                                     → ignore
```

默认 `k=1.35`、`KMax=1.667`。因此默认关系是小群 ← 中群 ← 大群；大群与
小群的体型比超出窗口，双方忽略。如果把小群体型拖到窗口另一侧，双方角色
仍会自动反转。方向性关系是派生结果，不为 N 个鱼群维护 N×N 参数矩阵。

退出边界带使用 `δ=0.10` 滞回。同群永不捕食。威胁由有效
关系中的捕食者进入邻近范围触发，与目标选择和捕获冷却无关。

捕食 steering 分为两个同时存在的连续力层：

- 大范围只在局部 hunt radius 内感知可捕食个体，以局部猎物群心施加捕猎
  凝聚力；局部为空时不施加捕猎力，继续使用普通 Boid 三力。
- 较小的 `detectionLength × burstRadiusFactor` 内选择近身目标，优先选择
  指向与捕食者当前速度方向夹角最小的猎物，夹角相同时选择更近者，并施加
  独立的高权重 burst 加速度。

hunt/panic radius 由当前鱼群动态 `cohesionRadius × detectionLengthFactor`
得到，默认倍率 `0.45`；捕食和恐慌在此范围外都不会触发。`pursuitWeight`
（面板显示为“捕猎凝聚 weight”）、`burstRadiusFactor` 和
`burstWeight` 均可实时调节。捕食者只在 burst 目标存在时获得 `1.35×`
速度上限，逃逸者上限为 `1.15×`。
捕获距离使用双方真实渲染体长之和的一半。捕食模式以默认每个捕食鱼群
`0.8 次/秒` 的目标速率反推每鱼独立冷却，并用种子随机数打散相位。
鱼群的 `captureRateMultiplier` 与相对 `referenceCruiseSpeed=0.23` 的速度
倍率共同缩短冷却；初始化、每次捕获后和指标面板必须使用同一计算。
捕获发生时从猎物死亡点发射可调的立方碎片，并在捕食者处产生咬合闪光。
每条有向捕食关系额外累计追击开始、追击帧、放弃、捕获、平均追击时长、
捕获追击时长、爆发秒数和捕获转化率。面板同时显示理论闭合时间；只有闭合
速度非正时警告，不使用未经实测支持的 `panic × 1.4` 硬阈值。

## 4. 主项目与隔离子实验

### Main Aquarium

默认打开主水族馆。它启用开放体型规则、捕获和捕获特效；死亡永久减少
当前种群，只有显式 reset/rebuild 才重新建立初始数量。它用于观察任意
数量鱼群形成的实时食物网。鱼群编辑器一次只展示一个群体：
使用前后箭头切换，通过加减按钮增删；形态、运动、群游和出生参数在该鱼群
内部分类。编辑器同时显示该群当前自动角色及其对其他鱼群的派生关系。
三个核心 Boid 规则分别使用独立面板栏：每栏依次显示当前鱼群的实际
`radius`、半径控制项和 `weight`。radius 决定纳入哪些邻居，weight 决定
对这些邻居施加多强的转向。Cohesion radius 等于按数量、缸体积和
`targetNeighbors` 动态反推的 `neighborRadius`，因此不伪造额外固定倍率。

`wallMargin` 是鱼心的硬边界内缩，并参与缸壁 collider 厚度；`edgeSoftness`
是到缸壁或静态障碍的软转向带，距离越近斥力越强。`crossSeparationScale`
只控制不同鱼群之间按较大体型缩放的连续斥力半径，不参与捕食关系判断。
同群 cohesion 只作用于动态 `cohesionRadius` 内的局部同群邻居；局部没有
邻居时不再施加指向实时全群群心的隐藏兜底力。

### Predation Mode

捕食模式开启真实捕获，但没有补充或繁殖。被捕获个体永久死亡，当前数量
单调减少，直到操作者显式 reset/rebuild。未来如果增加繁殖，必须以种群
当前数量和明确生态条件驱动，不得重新实现“按目标数量补缺口”。

### Ecology Mode

生态淘汰模式采用开放体型关系并允许捕获。每鱼保存独立能量：

```text
basalDrain = basalRate / size ^ basalSizeExponent
```

默认 `basalRate=0.020`、指数 `0.75`；实际代谢再乘该鱼群的
`metabolismMultiplier`。小/中/大群浮游摄食系数为
`1.0 / 0.25 / 0.01`，大群几乎只靠捕食。浮游资源按 logistic 方程增长，
摄食受当前资源和半饱和参数限制；捕获按猎物体型为捕食者补能。所有容量、
增长、摄食、转化、代谢和捕获补能参数都在注册表与面板中。

该项目启用隔离的 trait coupling：体型会压低持续速度与转向，但捕食仍使用
同一套“大范围捕猎凝聚 + 小范围 burst”两层力。运行态只有
`cruise / burst / evade`，没有独立冲刺 stamina，也没有潜伏或恢复阶段。
burst 可产生额外能量消耗；鱼只会因生态 energy 归零而饿死，不会因 burst
耗尽而死亡。只剩一个种群时冻结模拟并报告胜者；全部灭绝时冻结为
`collapse`。

## 5. 独立 Game 模式

Game 模式使用专属 UI，并在进入该项目时隐藏完整实验调试面板。玩家只看到
关卡说明、蓝鱼存活率、倒计时、结算，以及蓝鱼的速度、体型和耐力。

### 5.1 三角形控制器与乘法继承

三角形的三个方向代表速度、体型和耐力。领域真值是非负重心权重：

```text
w_size + w_stamina + w_speed = 1

m(w) = 0.5 + 1.5w    当 w ≤ 1/3
m(w) = 0.75 + 0.75w  当 w ≥ 1/3
```

中心点 `(1/3, 1/3, 1/3)` 精确映射为三项 `×1`。任一顶点把偏好轴映射为
`×1.5`，另外两轴映射为 `×0.5`；边中点为 `×1.125 / ×1.125 / ×0.5`。
UI 坐标只负责求重心权重，不直接作为领域状态。

| 玩家方向 | 主要模拟映射 | 主要代价 |
| --- | --- | --- |
| 速度 | 蓝鱼巡航速度、最高速度和捕获处理时间 | 高速提高代谢压力 |
| 体型 | 蓝鱼渲染尺寸、捕食关系、捕获距离、持续速度与转向 | 大体型更慢、更难转向，并增加代谢成本 |
| 耐力 | 降低蓝鱼 `metabolismMultiplier` | 不提高觅食率，也不直接回血 |

设本代三项系数为 `M_g`，已成功继承的累计系数为 `C_(g-1)`，则当前预览与
成功封代后的新累计值为：

```text
previewCumulative = C_(g-1) ⊙ M_g
成功封代：C_g = previewCumulative
本代实际表型 = 理想基础参数 ⊙ previewCumulative
```

其中 `⊙` 表示逐项相乘。单位累计系数和三角形中心点都表示“不改变理想
基础参数”。累计系数不做静默 clamp；同一轴先 `×1.5`、下一代再 `×0.5`
会得到 `×0.75`。表型从同一套理想基础参数重新派生，之后再应用已声明的
体型耦合公式；禁止把上一关已经变形的配置当作下一关基准。

只有蓝色玩家鱼群消费这些映射。NPC 鱼群、资源、捕获倍率和其他实验参数由
关卡配置固定，玩家不能通过 Game UI 修改。

### 5.2 状态机、失败与继承

```text
TUNING → RUNNING → VERDICT → INHERIT → 下一关 TUNING
                    └─失败────────────→ 当前关 TUNING
第三关 INHERIT → COMPLETE
```

- 每关只有开局的 `TUNING` 可以移动本代三角点。点击开始后只提交一次，并
  锁定到结算。
- 蓝鱼提前灭绝或倒计时结束时进入 `VERDICT`。胜负只使用真实存活率与真实
  死因统计。
- 失败不进入 `INHERIT`，不追加 `GenerationRecord`，也不修改累计系数。
  重试保留失败时的三角位置和本代系数，重建当前关生态，再开放本关三角点；
  后两关失败结算另提供完整谱系重开，避免继承路线软锁。
- 每次进入 Game、失败重试、进入下一代或重新实验都会创建新的 attempt seed，
  因而三群鱼的出生位置、初速度和 wander 相位会变化；同一尝试从 `TUNING`
  到 `RUNNING` 复用该 seed，玩家点击开始时不会看到场景跳位。
- 成功追加一条不可变记录，并把本代系数乘入累计系数。下一关以理想基础参数
  和新累计系数重算表型，把新一代三角点重置到中心，再开放一次调整。旧历史
  方案中“改过的单项在下一代永久锁死”的规则不适用于本模式。
- 继承参数与记录，不继承活鱼个体、当前能量或上一关存活数量。
- Game 状态仅保存在页面内存中，不使用调试面板的本地配置存储。刷新或
  **重新实验**会清空谱系、恢复单位累计系数，并回到 L1 的三角形中心点。
- 全流程禁止繁殖和自动补鱼。只有进入关卡、重试或完整重开会显式重建种群。

### 5.3 三关规格

| 关卡 | 生态压力与策略引导 | 时长 | 获胜存活率 |
| --- | --- | ---: | ---: |
| L1 · 匮乏年代 | 有捕食者，浮游勉强充饥；引导适度减小体型、增加耐力或速度 | 90 秒 | 35% |
| L2 · 黄金时代 | 浮游不足、捕食者很少、出现原本平行的鱼；引导增大体型捕食它们 | 75 秒 | 75% |
| L3 · 后疫情时代 | 食物与捕食者都少，整体活动降低；引导增加耐力 | 105 秒 | 50% |

每次尝试都从 `LevelSpec` 重建无繁殖生态。L1 允许较低剩余比例过关；L2 要求
高剩余比例；L3 用较长的低活动期检验长期代谢。

### 5.4 生产模拟器平衡基线

以下不是写死给玩家的“标准答案”，而是证明三关存在一条可解释、可继承的
完整通路。三角权重和倍率都按“体型 / 耐力 / 速度”排列：

| 关卡 | 本代三角权重 | 本代倍率 | 成功后的累计倍率 |
| --- | --- | --- | --- |
| L1 | `0.2 / 0.4 / 0.4` | `0.8 / 1.05 / 1.05` | `0.8 / 1.05 / 1.05` |
| L2 | `0.8 / 0 / 0.2` | `1.35 / 0.5 / 0.8` | `1.08 / 0.525 / 0.84` |
| L3 | `0.0625 / 0.875 / 0.0625` | `0.59375 / 1.40625 / 0.59375` | `0.64125 / 0.73828125 / 0.49875` |

生产 `ExperimentSimulation` 在 `dt=1/30`、种子
`1001/2003/3001/4001/5003` 下的结果：

| 关卡 | 正确路线 | 中立 / 错误路线 | 关键机制证据 |
| --- | --- | --- | --- |
| L1 | 平均存活 `75.5%`，最低 `60%`，`5/5` 通过 | 中立平均 `7%`；极端耐力平均 `8%`，均为 `0/5` 通过 | 正确路线平均被捕食 `9.8`；只押单项仍会被捕食或饿死，不能带着 L2 软锁过关 |
| L2 | 平均存活 `96.5%`，最低 `95%`，`5/5` 通过 | 中立与继续缩体均 `0%` | 正确路线平均捕食平行鱼 `52.0`；移除肉食供能或捕获都会失败 |
| L3 | 存活 `100%`，`5/5` 通过 | 中立与错误速度均 `0%` | 错误路线 40 条全部饿死，死亡压力来自长期能量而非捕食者 |

玩家界面实际使用 `dt=1/60` 与逐关种子 `1001/2001/3001`。同一路线分别
存活 `27/40`、`39/40`、`40/40`；三关的中立/错误对照都未过关。L2 实际
捕食 48 条平行鱼。任何关卡参数或表型耦合变化后，都必须重新运行搜索工具
并更新本节，不能沿用旧报告。

平衡验收不是“打印一个分数”就算通过。面向教学路线的搜索会先过滤不符合
本关引导的三角点，上一关未通过的谱系不能进入下一关；发布门禁还要求校准
谱系中的每一关同时满足：

- 推荐选择在五个确认种子上 `5/5` 通过；
- 从该关真实继承底本出发，中心点必须 `0/5` 通过；
- L1 的“极端缩体与耐力”、L2 的“继续缩体”、L3 的“只押速度”也必须
  `0/5` 通过。

任一条件不满足，命令以非零状态退出：

```bash
npm run test:balance
```

普通 `npm test` 只执行快速逻辑回归；上述发布门禁会运行 45 次完整生产模拟，
因此单独保留。完整候选搜索仍可用
`node tools/game_balance_search.mjs` 运行，并将每关的非对称校准点加入粗网格，
避免为了表示 L3 的 `1/16` 权重而暴力展开整个高分辨率三角网格。

这些检查是对“至少存在一条清晰且每代选择有效的教学路线”的发布证明，不是
玩家看不到的附加胜利条件。实际游戏仍只用界面公开的存活率判胜。玩家若在
前一代提前投资体型或耐力，从而让后代以中心点过关，属于乘法继承带来的合法
跨代策略；禁止这种前置投资反而会让“上一代影响下一代”变成装饰。

## 6. 障碍、刚体与相机

障碍预设由两个穿孔面板、两个矩形障碍和缸体组成。一个 `ObstacleSpec`
同时生成 Three.js 网格、Rapier collider 和解析 SDF。默认 `0.05m` 距离场
在六面各带一圈 padding；采样和中心差分在边界 clamp。穿孔区域或靠近表面
时直接使用解析 SDF，避免低分辨率把洞口抹平。

动态圆环、立方体、柱子与底座由 Rapier 处理。穿孔物体使用挂在同一刚体上
的多个凸 collider；鱼仍是纯 boid，只在 AABB 预筛后查询动态物体，并通过
偏心 `applyImpulseAtPoint` 推动它们。

鱼观察采用 Pointer Events：单击活鱼后主相机保持全局视角，左下角打开真实
渲染的第三人称特写窗。窗口可进入两种全屏状态：

- `closeup`：从鱼的侧前方近距离构图，使用独立可调距离、侧移、高度与 FOV；
  三个位置偏移按鱼的实时 size 缩放，保持不同体型的占屏比例一致。
- `follow`：沿用原单击鱼后的后方跟随构图。

两种状态的位置与朝向均独立阻尼，ESC 退出并清除选择。鱼头第一人称、
鼠标转头和双击入口已删除。目标死亡后优先切到最近同群活鱼，否则退出。

时间倍率除面板 `time scale` 外提供按住型全局快捷键：按住 Enter 临时使用
`2×`，按住 Space 临时使用 `0.2×`；松开恢复 `1×`。200ms 内连续按两次
Space 会额外退出 closeup/follow、清除选择并回到全局原视角，不改变 Space
的按住慢放语义。同时按住时最后按下者优先，释放后回到仍按住的另一个倍率；
窗口失焦也恢复 `1×`，避免临时倍率卡住。输入控件获得焦点时不劫持这些
按键；暂停仍可在面板把 time scale 调为 `0`。

默认三群的 Boid 权重统一为 separation/alignment/cohesion
`0.80 / 0.45 / 0.40`，总 steering 上限为 `5.2`。动态半径在默认小群下
约为 separation/alignment/cohesion `0.139 / 0.335 / 0.628`；这是默认缸体
三个轴均扩大一倍后，由目标邻居密度自动推导的结果。

参数盘“可视化”分组提供两个开关：`0号鱼 · 同群三力半径` 与
`0号鱼 · 捕食/逃逸半径`。打开后，以编号 0 的活鱼为圆心，在缸内绘制
对应线框球，半径直接取当前派生值；0 号鱼死亡时自动隐藏。本地开发必须
通过 `npm run dev` 或 `npm run dev:http` 访问，不能直接打开 `index.html`。

## 7. 公共调试接口

浏览器控制台暴露：

```js
window.experiment = {
  config,
  stageConfig,
  applyConfig,
  exportConfig,
  importConfig,
  reset,
  metrics,
  spawnRigidBody,
  setProject
}
```

该接口与面板属于实验调试模式，不构成 Game 的玩家 API。面板还提供默认值
恢复、JSON 导入导出、本地保存、seed 复制、实验报告导出、
实时关系矩阵、派生半径、捕获距离、追击转化、能量、浮游账本和
实测邻居数。每个数值参数保留独立的量程窗口：可围绕当前值递归缩放，
量程窗口跨面板重建保留，但始终受参数注册表的安全上下限约束。

## 8. 阶段门与验收

三群必须通过群形、体型派生捕食关系和静态距离场性能检查。

自动测试放在可提交的 `src/*.test.js`，覆盖配置注册、半径、空间哈希、
体型关系、威胁、速度与捕获、永久死亡、种子复现、生态能量、灭绝结局
和距离场梯度。Game 测试还必须覆盖重心权重、分段倍率、三角形中心的单位
映射、关内锁定、无 clamp 累计乘法、失败不封代、下一代中心重置、关卡重建
和完整重开。纯模型测试不导入
Three.js；渲染相关模拟测试单独放置。交付前必须通过 `npm test` 与
`npm run build`，再做 680 条鱼、生态账本、洞口拥堵、
三类刚体、鱼观察窗口与两种全屏视角及三大现代浏览器冒烟。

完整参数清单由统一注册表生成，禁止手工维护：

```bash
node tools/export_experiment_parameters.mjs --write
```

输出为 `docs/EXPERIMENT_PARAMETERS.md`。该文件列出实验/调试注册表、Game
固定关卡参数、蓝鱼理想基础值与表型耦合，但不是 Game 玩家界面。

## 9. 需求验收状态

| 需求 | 状态 | 可复核证据 |
| --- | --- | --- |
| Q1：提高进食恢复，并把 40% 平均分给同种鱼 | 已完成 | `ecology.forageEnergyMultiplier=2.2`、`energyShareFraction=0.4`；浮游、浮尸与捕食补能进入同族共享池；模拟测试覆盖恢复与均分 |
| 浮游在项目切换后恢复开启 | 已完成 | Aquarium 与 Ecology 预设显式恢复 `plankton.enabled=true`；Obstacle 仍按隔离实验关闭生态，切回后不会遗留关闭状态 |
| 速度倍率统一影响全部捕获冷却与指标 | 已完成 | 初始化、捕获后重置、指标面板统一使用 `captureSpeedFactor` 与鱼群捕获倍率 |
| 独立 Game 玩家界面 | 已完成 | Game 隐藏完整调试盘，只显示三角控制器、关卡、存活率、倒计时、死因结算和三代谱系 |
| 每关只在开局调整一次 | 已完成 | `TUNING` 可调，`RUNNING` 立即锁定；失败回本关调参，成功后下一代重新开放一次 |
| 三项系数乘法继承 | 已完成 | 成功才提交 `cumulative = inherited ⊙ round`；失败不提交；每代从理想基础值重算，不继承活鱼与能量 |
| 三关生态与策略区分 | 已完成 | L1 反制极端缩体且保留适度缩体路线；L2 必须跨体型窗捕食平行鱼供能；L3 以低浮游量检验耐力 |
| 三代平衡发布门禁 | 已完成 | `npm run test:balance` 以五种子验证推荐路线 `5/5`，并要求每关中心点和指定错误选择均 `0/5`；失败时返回非零状态 |
| 不繁殖、不补鱼 | 已完成 | Game 每次尝试重建固定种群，运行中死亡永久；配置和运行时均无补充机制 |
| 完整参数清单 | 已完成 | `tools/export_experiment_parameters.mjs --write` 从注册表与 `LevelSpec` 生成，不靠手工抄表 |

关卡搜索使用 `tools/game_balance_search.mjs` 直接运行生产
`ExperimentSimulation`，同时记录种子、存活率、死因、捕获数和完整三代乘法
路径；不得用独立简化方程替代最终验收。
