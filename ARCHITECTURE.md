# experiment 分支覆盖条款（2026-07-25）

> 本节是 `experiment` 分支的最高优先级实施约束。该分支只交付
> **Vite + Three.js 的纯 Web 实验版**，不使用 Godot、Electron、Tauri
> 或浏览器套壳。详细、可执行的技术基线见
> [`EXPERIMENT-DEVELOPMENT.md`](./EXPERIMENT-DEVELOPMENT.md)。
> Web 入口默认运行主水族馆；地图刚体和生态淘汰是隔离子实验。
> 主项目角色由实时体型开放推导。营养级联、holding、尺寸窗口和配对判据
> 已按设计决定完整删除。生态淘汰允许真实饥饿与灭绝，但不恢复历史四回合流程。
> Web 实验版不按目标数量补鱼；死亡永久生效，直到操作者显式重置或重建。
> 当前不实现繁殖，未来繁殖必须作为由实际种群状态驱动的独立机制设计。
> 捕食采用“大范围猎物群凝聚 + 小范围高权重 burst”两层连续力；不使用
> `stalk → burst → recover` 或独立冲刺耐力状态机。
> Web 观察流程为“单击鱼打开左下角第三人称观察窗 → 选择特写或跟随全屏
> → ESC 返回全局”；不再使用鱼头第一人称和双击入口。
>
> 本文件下方的 36 小时 Godot 方案仅保留为项目历史，不适用于
> `experiment` 分支；如有冲突，以本节和实验开发文档为准。

# evolution — 架构与分工 v2（历史方案）
*2026-07-23 深夜定稿 · 截止 07-25 08:00 · 本文件取代 v1，是唯一宪法*

> 分工原则：**Billy 定 what（产品/效果/拍板），北辰定 how（模拟与代码
> 架构），三金定现场（场景皮肤/配乐/VR）。** 代码架构内部分层是北辰的
> 领地，本文件只钉死：产品规则、四个接口的数据形状、时间表、红线。

---

## 1. 产品定义（游戏怎么玩、怎么赢）

**一句话：** 观众亲手给一群鱼的世界施加三代压力，最后眼睁睁看着
自己第一代的选择害死第三代的鱼 —— 而且回不了头。

### 1.1 三局结构（现实映照：匮乏年代 → 黄金年代 → 泡沫破碎）
| 局 | 生存问题 | 预期的"唯一解"方向 |
|---|---|---|
| L1 饥荒 | 食物极少 | 压低耐力需求（省着活） |
| L2 黄金 | 物资充裕、有可捕食的小鱼 | 提速度（不然吃不到肉） |
| L3 下行 | 食物尚可，出现"平行鱼"（原本与你同级） | 若前两局曾压过体型 → 被平行鱼吃。债务显形。 |

### 1.2 回合状态机（四个阶段，Billy 的主循环）
```
TUNING 调参 → RUNNING 演算 → VERDICT 结算 → INHERIT 封代 → 下一局
```
- **TUNING**：滑块自由调（受遗产锁限制）；确认前可无限重调（"刷新"）。
- **RUNNING**：参数锁死；玩家控制演算速度（1×~N× 快进）；计时到点。
- **VERDICT**：胜负判定 + 本局文案 + 死因统计展示。
- **INHERIT**：本局改过的参数进锁定表；下一局这些滑块变灰。
（"相位定义"就是指这四个阶段的进入/退出条件——归 Billy。）

### 1.3 特征与耦合（玩家面对的滑块）
- **速度 speed** · **大小 size** · **耐力 stamina**（=饥饿需求，越高越能扛饿）
- （可选第四项）**觅食 foraging**：找食物的优先权重
- 耦合规则（北辰实现成什么数学由他定，效果必须是）：
  速度↑→耐力承压（更容易饿）；耐力要求降→大小随之降；大小↑→速度/灵活↓。
  **改一个，别的当场动，全部肉眼可见**（大小=缩放，速度=游速，
  耐力=饿死率/进食频率——耐力最难可视，北辰重点想这个）。
- **遗产锁：上一局玩家动过的参数，本局锁死。** 这是"债"的机械本体。

### 1.4 三种死法（与三特征一一对应）
1. **被吃**：遇到比自己大一圈的鱼，会被追；被追上→吃掉动画→消失。
   （太大也有代价：钻不进狭缝类地形——L3 可用。）
2. **追不上**：速度不足 → L2 吃不到猎物 → 转化为饿死。
3. **饿死**：耐力需求 × 食物供给不匹配 → 饥饿条走完消失。

### 1.5 食物形态 —— 【fable 建议，Billy 已可直接采纳或推翻】
**双轨制：浮游生物（粒子食物）做饥饿经济的主干，猎物小鱼做戏剧。**
- 把"鱼粮"重新包装成**浮游生物/磷虾群**（漂浮粒子），怪感消失；
  它有一个纯粹的旋钮（生成速率），三金每局填一个数就能表达
  饥荒/丰饶，且观众 2 秒内看懂（食物雨稀 vs 密）。
- 纯猎物小鱼做经济主干在 36h 内几乎不可调平（猎物自己也是活的模
  拟，双层耦合），但它在 L2"提速吃肉"和 L3"平行鱼吃你"里不可替代
  —— 所以小鱼只做**大小戏剧**，不做热量账本。

### 1.6 胜利条件 —— 【fable 强烈建议 B，理由见下】
**B：鱼不繁殖。每局固定初始数量（实验室叙事：局间由"实验室"补鱼，
继承的是参数不是个体）。胜利 = 计时结束时存活数 ≥ 阈值%（每局可不同）。**
- 不选 A（繁殖+种群比例）的原因：繁殖系统 = 出生逻辑+种群动力学
  平衡+新个体参数继承规则，36h 调不平，且会稀释"遗产=参数"的叙事。
- A 的精髓（第一局暴涨→第二局饿死一片）在 B 里可部分找回：
  **幸存数结转**（上局勉强赢→下局起始鱼更少）——标记为**有时间才做**。
- 实验室叙事本身贴题：**遗产是参数，不是鱼。**

### 1.7 数量参考（起点值，北辰随时改）
玩家鱼群 25–40 · 掠食/平行鱼 8–15 · L2 猎物小鱼群 15–20 ·
浮游粒子按生成率不设上限但设存活上限。

---

## 2. 四个接口契约（**现在冻结**；改字段需 Billy 拍板 + 涉事双方知情）

### 2.1 TraitsSpec（Billy → 北辰）：玩家滑块 → 模拟
```js
{ speed: 0..100, size: 0..100, stamina: 0..100, foraging?: 0..100 }
// 北辰负责：翻译成 boid 权重组合 + 新机制参数；耦合数学；可视化。
// 面板：共用现有 addParam 面板，但暴露的是这套新参数（不是裸 boid 参数）。
```

### 2.2 LevelSpec（形状 Billy 定，内容三金填，北辰消费）
```js
{ id, label, story,                    // 文案（VERDICT 屏用）
  durationSec, winSurvivalPct,         // 时长与胜利阈值
  plankton: { spawnRate },             // 饥饿经济旋钮
  preyFish:   { count, sizeClass },    // 可捕食小鱼（L2+）
  rivalFish:  { count, sizeClass },    // 掠食/平行鱼
  terrain?: { narrowGaps: bool },      // 狭缝类地形（可选）
  dressing: { bg, propsRef, musicRef } // 场景皮肤/配乐（三金资产）
}
```

### 2.3 RoundReport（北辰 → Billy）：一局结束交出什么
```js
{ survivors, initial,                        // 数量账
  deaths: { eaten: n, starved: n },          // 死因分布 —— 指认时刻的证据
  finalTraits: {...},                        // 本局终值
  events?: [ {t, type, note} ]               // 可选：给文案用的时间线
}
```
**死因必须来自真实模拟**（谁吃了谁、谁饿死），不许判定表编造 ——
这是 B1"实时力场"决定的红利，也是指认时刻可信的根。

### 2.4 GenerationRecord（Billy 内部，lineage，append-only）
```js
{ gen, traitsBefore, traitsAfter, changed: ['speed'],  // → 下局锁定表
  level, verdict, report }                              // 冻结，不可回档
```

### 2.5 VR（三金，隔离带）
只消费两样东西：**scene 句柄 + 鱼群中心的相机挂载点**（北辰暴露）。
Godot OpenXR/PICO 适配代码归三金。**硬规则：主 demo 在普通屏幕上必须 100% 成立，
VR 任何时刻可整体拔掉不留残根。** 帧预算问题等设备接上后再谈，
现在一切以电脑端为准。

### 2.6 运行平台（07-24 决议）
**主交付改为 Godot 4 原生 Android APK。** `godot/` 是主项目；仓库根目录
原 Three.js 版本只作为 boids 算法与视觉参考，不再承担正式运行入口。

- 普通桌面/Android 屏幕模式必须完整跑通四阶段与三局。
- PICO 通过独立 OpenXR 导出预设与适配节点接入，不允许模拟、lineage
  或 UI 状态机直接依赖 XR。
- Android 以 `arm64-v8a`、JDK 17、Compatibility/OpenGL 渲染为基线。
- PICO/OpenXR 未接入前，普通 Android APK 不得宣称为 XR 构建。

---

## 3. 分工地图

| 人 | 拥有 | 交付物 | 找谁要什么 |
|---|---|---|---|
| **Billy** | 产品规则、四阶段主循环、遗产锁、lineage、UI（先 DOM 覆盖层起工，后期视情况进 canvas）、结算文案框架、最终拍板 | game/ui 层；指认时刻那一屏（端到端拥有） | 向北辰要 RoundReport；向三金要每局 story/dressing |
| **北辰** | 模拟与代码架构全权（含现有 boids 代码的处置、模块化方案、集成节奏建议） | 特征翻译+耦合、饥饿/进食/被吃机制、可视化、RoundReport、相机挂载点、每回合文案钩子 | 向 Billy 要 TraitsSpec 效果验收；向三金要 LevelSpec 内容 |
| **三金** | 三局场景皮肤（颜色/风格区分即可，不用精细）、配乐、现场物料、VR 链路 | L1/L2/L3 的 dressing 资产 + LevelSpec 内容值；VR 演示模式 | 向 Billy 要 LevelSpec 形状确认；向北辰要 scene/相机句柄 |

- 鱼的**外观规则**（trait→形态映射）暂归 Billy 出规则、北辰实现；
  涉及 3D 建模渲染细节先问三金能做什么。
- 集成节奏由北辰评估建议，Billy 认效果；冲突 Billy 拍板。

---

## 4. 36 小时作战表（现在 = H0）

| 时段 | Billy | 北辰 | 三金 |
|---|---|---|---|
| H0–4 | 冻结本文件；DOM 版 UI 骨架（用**假 RoundReport** 先跑起来，不等任何人） | 决定代码处置方案；模块化手术；TraitsSpec 翻译打通 | L1 场景动工；音乐选型 |
| H4–12 | 调参 UI + 遗产锁 + lineage | 饥饿/浮游/进食/被吃机制，全部可视 | L1–L3 dressing + LevelSpec 内容初值 |
| H12–20 | **整合里程碑：L1 一局端到端可玩**（真数据换掉假数据） | 同左（联调） | 同左（联调）；VR 只许在此后碰 |
| H20–30 | 三局全通 + 平衡调参（"唯一解"手感）+ 结算文案 + **展台重开键** | 平衡支持 + 性能 | L2/L3 精修 + 配乐落位 |
| H30–36 | 三分钟路演彩排 ×2 + 缓冲 | 缓冲/救火 | VR go/no-go（不影响主线） |

**每 6 小时一次"指认检查"**（Billy 主持，5 分钟）：现在这个版本里，
玩家能不能指着第一局说"是那次害的"？答不上来就调整优先级。

## 5. 红线

1. **接口冻结**：§2 的四个形状从现在起改字段=Billy 拍板+涉事双方知情。
2. **VR 永远可拔**：每晚必须有一次纯屏幕全流程跑通。
3. **指认时刻那一屏归 Billy 端到端拥有**，北辰三金向它交数据，
   它从 H0 起就有假数据版本。
4. **展台模式**：一键重开、连跑十轮不崩，H20–30 内完成，归 Billy。
5. 任何新想法先过一个问题：**它让"指认时刻"更疼吗？** 不让就砍。
